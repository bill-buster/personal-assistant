#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Pre-push hook: Run full preflight checks before pushing

echo "🚀 Pre-push checks..."

# Run full preflight (includes build, leak check, smoke test)
npm run preflight || {
    echo ""
    echo "❌ Pre-push checks failed. Please fix issues before pushing."
    exit 1
}

echo ""
echo "✅ Pre-push checks passed!"
